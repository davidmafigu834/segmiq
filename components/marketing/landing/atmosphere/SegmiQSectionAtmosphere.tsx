import type { AtmosphereTone } from "@/components/marketing/landing/atmosphere/wave-field";

export type { AtmosphereTone };

/** Section atmosphere retired. Kept as a no-op so existing imports stay valid. */
export default function SegmiQSectionAtmosphere(_props: { tone: AtmosphereTone }) {
  return null;
}
