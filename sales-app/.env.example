# API base URL (no trailing slash)
# Production: use www — apex segmiq.com redirects and breaks Capacitor POST bodies on Android.
VITE_API_BASE=https://www.segmiq.com
# Local dev:
# VITE_API_BASE=http://localhost:3000
